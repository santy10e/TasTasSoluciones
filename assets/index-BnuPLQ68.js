(function () { const t = document.createElement("link").relList; if (t && t.supports && t.supports("modulepreload")) return; for (const o of document.querySelectorAll('link[rel="modulepreload"]')) r(o); new MutationObserver(o => { for (const n of o) if (n.type === "childList") for (const c of n.addedNodes) c.tagName === "LINK" && c.rel === "modulepreload" && r(c) }).observe(document, { childList: !0, subtree: !0 }); function s(o) { const n = {}; return o.integrity && (n.integrity = o.integrity), o.referrerPolicy && (n.referrerPolicy = o.referrerPolicy), o.crossOrigin === "use-credentials" ? n.credentials = "include" : o.crossOrigin === "anonymous" ? n.credentials = "omit" : n.credentials = "same-origin", n } function r(o) { if (o.ep) return; o.ep = !0; const n = s(o); fetch(o.href, n) } })(); const g = document.getElementById("progress-bar"), l = document.getElementById("float-cta"), E = document.getElementById("header"); window.addEventListener("scroll", () => { const e = document.documentElement.scrollTop, t = document.documentElement.scrollHeight - window.innerHeight; g.style.width = e / t * 100 + "%", E.style.boxShadow = e > 20 ? "0 4px 20px rgba(28,58,46,.08)" : "none" }); const p = document.getElementById("footer"), v = new IntersectionObserver(e => { e.forEach(t => { t.isIntersecting ? l.classList.remove("show") : document.documentElement.scrollTop > window.innerHeight * .6 && l.classList.add("show") }) }, { root: null, threshold: .1 }); v.observe(p); window.addEventListener("scroll", () => { const e = document.documentElement.scrollTop; !p.getBoundingClientRect().top < window.innerHeight && l.classList.toggle("show", e > window.innerHeight * .6) }); const a = document.getElementById("ham"), d = document.getElementById("mobile-menu"); a.addEventListener("click", () => { a.classList.toggle("open"), d.classList.toggle("open") }); d.querySelectorAll("a").forEach(e => { e.addEventListener("click", () => { a.classList.remove("open"), d.classList.remove("open") }) }); document.querySelectorAll('a[href^="#"]').forEach(e => { e.addEventListener("click", t => { const s = document.querySelector(e.getAttribute("href")); s && (t.preventDefault(), s.scrollIntoView({ behavior: "smooth", block: "start" })) }) }); const w = new IntersectionObserver(e => { e.forEach(t => { t.isIntersecting && t.target.classList.add("up") }) }, { threshold: .08 }); document.querySelectorAll(".reveal, .reveal-r").forEach(e => w.observe(e)); function b(e, t, s = 1600) { const r = performance.now(), o = n => { const c = Math.min((n - r) / s, 1), y = 1 - Math.pow(1 - c, 3); e.textContent = Math.floor(y * t), c < 1 ? requestAnimationFrame(o) : e.textContent = t }; requestAnimationFrame(o) } const h = new IntersectionObserver(e => { e.forEach(t => { t.isIntersecting && (document.querySelectorAll(".num-count").forEach(s => { b(s, parseInt(s.dataset.target)) }), h.disconnect()) }) }, { threshold: .5 }), m = document.querySelector(".num-count"); m && h.observe(m); const L = new IntersectionObserver(e => { e.forEach(t => { t.isIntersecting && t.target.querySelectorAll(".feat-bar-fill").forEach((s, r) => { setTimeout(() => { s.style.width = s.dataset.width }, r * 150) }) }) }, { threshold: .3 }); document.querySelectorAll(".plan-wrap").forEach(e => L.observe(e)); document.querySelectorAll(".faq-trigger").forEach(e => { e.addEventListener("click", () => { const t = e.closest(".faq-item"), s = t.querySelector(".faq-answer"), r = t.classList.contains("open"); document.querySelectorAll(".faq-item").forEach(o => { o.classList.remove("open"); const n = o.querySelector(".faq-answer"); n.classList.remove("open"), n.style.opacity = "0" }), r || (t.classList.add("open"), s.classList.add("open"), setTimeout(() => s.style.opacity = "1", 50)) }) }); const u = document.getElementById("price-toggle"), I = document.getElementById("lbl-once"), q = document.getElementById("lbl-maint"), f = { s: { once: "$90", maint: '$90 <span style="font-size:.5em;opacity:.7">+ $19/mes</span>' }, b: { once: "$590", maint: '$590 <span style="font-size:.5em;opacity:.7">+ $39/mes</span>' }, p: { once: "$1,200", maint: '$1,200 <span style="font-size:.5em;opacity:.7">+ $79/mes</span>' }, e: { once: "$2,500", maint: '$2,500 <span style="font-size:.5em;opacity:.7">+ $149/mes</span>' } }; let i = !1; u.addEventListener("click", () => { i = !i, u.classList.toggle("on", i), ["s", "b", "p", "e"].forEach(e => { document.getElementById("price-" + e).innerHTML = i ? f[e].maint : f[e].once }), I.style.opacity = i ? ".5" : "1", q.style.opacity = i ? "1" : ".5" });
